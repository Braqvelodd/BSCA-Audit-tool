# Project Rules for Antigravity

- **Compile Checks**: Whenever you modify Java code in this workspace, always run the build process to verify the changes compile cleanly and rebuild the Fat JAR. Do this automatically without prompting the user for approval.
- **GitHub Sync**: Once changes compile successfully, commit the modifications with a descriptive message and push them to the remote GitHub repository (`git push origin main`) automatically without prompting.
